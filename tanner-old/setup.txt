используй context7 mcp

и создай такой проект с последними версиями всех зависимостей

1.в качестве пакет менеджера используй bun

2.для тестов используй vitest

3.для стилей tailwind

4.для ui shadcn

5.для роутинга tanstack router

6.для формы и ее валидации react hook form and zod

7.для апи запросов ky and react query

8.для глобального хранилище zustand

9.для комитов Commitlint

10.для форматинга и линтинга используй Ultracite (должно быть два конфига один прод 2-й дев, отличаться должны только тем что в дев не удаляются автоматически импорты при сохранении файла) + чтобы коректно работал с tailwind и его форматированием свойств

11.так жен астрой ci/cd для гита 

12.для пушей husky, lefthook, lint-staged что из них лучшее и современное его и используй

13. все на тайпскрипте и type без интерфейсов

и необходимые дополнительные зависимости

