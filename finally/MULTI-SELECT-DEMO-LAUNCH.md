# Запуск Multi-Select Demo

## Быстрый старт

1. **Установите зависимости** (если еще не установлены):
```bash
pnpm install
```

2. **Запустите dev сервер**:
```bash
pnpm run dev
```

3. **Откройте в браузере**:
```
http://localhost:5173/demo/multi-select
```

## Альтернативный способ

1. Откройте главную страницу: `http://localhost:5173`
2. Кликните на ссылку "View Multi-Select Demo"

## Что вы увидите

Интерактивную демо-страницу с 5 примерами:

1. ✅ **Базовый пример** - простой мультиселект
2. ✅ **С группировкой** - опции по категориям
3. ✅ **Overflow Behavior** - управление отображением
4. ✅ **Контролируемое состояние** - внешнее управление
5. ✅ **В форме** - с валидацией

## Файлы

- **Демо-страница**: `src/routes/demo/multi-select.tsx`
- **Компонент**: `src/shared/components/ui/multi-select.tsx`
- **Примеры**: `src/shared/components/ui/multi-select-new.example.tsx`
- **Документация**: `src/shared/components/ui/MULTI-SELECT-NEW-API.md`

## Возможности

- 🎨 Поддержка темной темы
- 📱 Адаптивный дизайн
- ⌨️ Полная поддержка клавиатуры
- ♿ Доступность (ARIA)
- 🎭 Анимации и переходы
- 🔧 TypeScript типизация

## Troubleshooting

### Ошибка типов роутов

Если видите ошибку `Argument of type '"/demo/multi-select"' is not assignable...`:

1. Остановите dev сервер (Ctrl+C)
2. Запустите снова: `pnpm run dev`
3. TanStack Router автоматически сгенерирует типы

### Порт занят

Если порт 5173 занят, Vite автоматически выберет другой порт (например, 5174).
Проверьте вывод в консоли.

### Компонент не отображается

1. Проверьте, что все зависимости установлены: `pnpm install`
2. Очистите кэш: `rm -rf node_modules/.vite`
3. Перезапустите dev сервер

## Дополнительно

### Сборка для продакшена

```bash
pnpm run build
```

### Предпросмотр сборки

```bash
pnpm run serve
```

### Запуск тестов

```bash
pnpm run test
```

## Навигация по демо

- **Главная**: `/`
- **Table Demo**: `/demo/table`
- **Multi-Select Demo**: `/demo/multi-select` ← Вы здесь!

Приятного использования! 🚀
