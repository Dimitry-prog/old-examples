# Руководство по внесению вклада

Спасибо за интерес к проекту Modern React Stack! Мы рады любому вкладу.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как внести вклад](#как-внести-вклад)
- [Процесс разработки](#процесс-разработки)
- [Стиль кода](#стиль-кода)
- [Коммиты](#коммиты)
- [Pull Requests](#pull-requests)
- [Отчеты об ошибках](#отчеты-об-ошибках)
- [Предложения функций](#предложения-функций)

## 🤝 Кодекс поведения

Участвуя в этом проекте, вы соглашаетесь соблюдать наш Кодекс поведения:

- Будьте уважительны и инклюзивны
- Принимайте конструктивную критику
- Фокусируйтесь на том, что лучше для сообщества
- Проявляйте эмпатию к другим участникам

## 🚀 Как внести вклад

### 1. Fork репозитория

Нажмите кнопку "Fork" в правом верхнем углу страницы репозитория.

### 2. Клонируйте ваш fork

```bash
git clone https://github.com/your-username/modern-react-stack.git
cd modern-react-stack
```

### 3. Добавьте upstream remote

```bash
git remote add upstream https://github.com/original-owner/modern-react-stack.git
```

### 4. Создайте ветку для изменений

```bash
git checkout -b feature/your-feature-name
```

### 5. Внесите изменения

Следуйте [стилю кода](#стиль-кода) и [правилам коммитов](#коммиты).

### 6. Запустите тесты

```bash
bun run test
bun run lint
bun run type-check
```

### 7. Commit изменения

```bash
bun run commit
# или
git commit -m "feat: add your feature"
```

### 8. Push в ваш fork

```bash
git push origin feature/your-feature-name
```

### 9. Создайте Pull Request

Откройте Pull Request из вашей ветки в main ветку оригинального репозитория.

## 💻 Процесс разработки

### Установка зависимостей

```bash
bun install
```

### Запуск dev сервера

```bash
bun run dev
```

### Структура проекта

```
src/
├── components/     # React компоненты
├── hooks/         # Кастомные хуки
├── lib/           # Утилиты
├── routes/        # Страницы (TanStack Router)
├── stores/        # Zustand stores
├── types/         # TypeScript типы
└── styles/        # Глобальные стили
```

### Добавление новых компонентов

1. Создайте компонент в соответствующей папке
2. Добавьте TypeScript типы
3. Напишите тесты
4. Обновите документацию
5. Экспортируйте из index.ts

Пример:

```typescript
// src/components/ui/MyComponent.tsx
import type { ReactNode } from 'react'

interface MyComponentProps {
  children: ReactNode
  variant?: 'default' | 'primary'
}

export function MyComponent({ children, variant = 'default' }: MyComponentProps) {
  return <div className={`my-component ${variant}`}>{children}</div>
}
```

### Добавление новых страниц

1. Создайте файл в `src/routes/`
2. Используйте `createFileRoute` из TanStack Router
3. Добавьте соответствующий layout
4. Обновите навигацию если нужно

Пример:

```typescript
// src/routes/my-page.tsx
import { createFileRoute } from '@tanstack/react-router'
import { MainLayout } from '@/components/layouts'

function MyPage() {
  return (
    <MainLayout>
      <h1>My Page</h1>
    </MainLayout>
  )
}

export const Route = createFileRoute('/my-page')({
  component: MyPage,
})
```

## 🎨 Стиль кода

### TypeScript

- Используйте `type` вместо `interface` где возможно
- Избегайте `any`, используйте `unknown` если тип неизвестен
- Используйте строгую типизацию
- Предпочитайте функциональные компоненты

```typescript
// ✅ Хорошо
type User = {
  id: string
  name: string
}

// ❌ Плохо
interface User {
  id: any
  name: any
}
```

### React

- Используйте функциональные компоненты
- Используйте хуки вместо классов
- Деструктурируйте props
- Используйте TypeScript для props

```typescript
// ✅ Хорошо
type ButtonProps = {
  onClick: () => void
  children: ReactNode
}

export function Button({ onClick, children }: ButtonProps) {
  return <button onClick={onClick}>{children}</button>
}

// ❌ Плохо
export function Button(props) {
  return <button onClick={props.onClick}>{props.children}</button>
}
```

### Стилизация

- Используйте Tailwind CSS классы
- Следуйте utility-first подходу
- Используйте `cn()` для условных классов

```typescript
import { cn } from '@/lib/utils'

// ✅ Хорошо
<div className={cn('base-class', isActive && 'active-class')} />

// ❌ Плохо
<div className={`base-class ${isActive ? 'active-class' : ''}`} />
```

### Именование

- **Компоненты**: PascalCase (`MyComponent`)
- **Функции**: camelCase (`myFunction`)
- **Константы**: UPPER_SNAKE_CASE (`MY_CONSTANT`)
- **Файлы**: kebab-case (`my-component.tsx`)
- **Типы**: PascalCase (`MyType`)

### Форматирование

Проект использует Biome для автоматического форматирования:

```bash
bun run format
```

## 📝 Коммиты

### Conventional Commits

Мы используем [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Типы коммитов

- `feat` - новая функциональность
- `fix` - исправление бага
- `docs` - изменения в документации
- `style` - форматирование, отсутствующие точки с запятой
- `refactor` - рефакторинг кода
- `perf` - улучшение производительности
- `test` - добавление или изменение тестов
- `build` - изменения в системе сборки
- `ci` - изменения в CI конфигурации
- `chore` - другие изменения

### Примеры

```bash
feat(auth): add OAuth2 integration
fix(api): handle timeout errors
docs: update installation guide
style: format code with prettier
refactor(hooks): simplify useAuth implementation
test(components): add Button tests
```

### Интерактивное создание коммита

```bash
bun run commit
```

## 🔄 Pull Requests

### Чеклист перед созданием PR

- [ ] Код следует стилю проекта
- [ ] Все тесты проходят
- [ ] Добавлены новые тесты для новой функциональности
- [ ] Документация обновлена
- [ ] Коммиты следуют Conventional Commits
- [ ] PR имеет описательное название
- [ ] PR описание объясняет изменения

### Название PR

Используйте тот же формат, что и для коммитов:

```
feat(auth): add OAuth2 integration
fix(api): handle timeout errors
```

### Описание PR

Используйте шаблон:

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Изменения
- Изменение 1
- Изменение 2

## Тестирование
Как были протестированы изменения

## Скриншоты
Если применимо

## Чеклист
- [ ] Код следует стилю проекта
- [ ] Тесты проходят
- [ ] Документация обновлена
```

### Процесс ревью

1. Автоматические проверки должны пройти
2. Минимум 1 одобрение от мейнтейнера
3. Все комментарии должны быть разрешены
4. Конфликты должны быть разрешены

## 🐛 Отчеты об ошибках

### Перед созданием issue

1. Проверьте, не была ли ошибка уже сообщена
2. Убедитесь, что используете последнюю версию
3. Соберите необходимую информацию

### Создание issue

Используйте шаблон Bug Report:

```markdown
**Описание ошибки**
Четкое описание проблемы

**Шаги для воспроизведения**
1. Перейти на '...'
2. Нажать на '...'
3. Увидеть ошибку

**Ожидаемое поведение**
Что должно было произойти

**Скриншоты**
Если применимо

**Окружение**
- OS: [e.g. Windows 10]
- Browser: [e.g. Chrome 96]
- Version: [e.g. 1.0.0]
```

## 💡 Предложения функций

### Перед созданием предложения

1. Проверьте, не было ли уже такого предложения
2. Убедитесь, что функция соответствует целям проекта
3. Подумайте о реализации

### Создание предложения

Используйте шаблон Feature Request:

```markdown
**Проблема**
Какую проблему решает эта функция?

**Решение**
Как вы видите решение?

**Альтернативы**
Какие альтернативы вы рассматривали?

**Дополнительный контекст**
Любая дополнительная информация
```

## 🧪 Тестирование

### Написание тестов

```typescript
import { describe, it, expect } from 'vitest'
import { render, screen } from '@testing-library/react'

describe('MyComponent', () => {
  it('renders correctly', () => {
    render(<MyComponent />)
    expect(screen.getByText('Hello')).toBeInTheDocument()
  })

  it('handles click events', async () => {
    const handleClick = vi.fn()
    render(<MyComponent onClick={handleClick} />)
    
    await userEvent.click(screen.getByRole('button'))
    expect(handleClick).toHaveBeenCalled()
  })
})
```

### Запуск тестов

```bash
# Все тесты
bun run test

# Конкретный файл
bun run test src/components/Button.test.tsx

# Watch режим
bun run test:watch

# С покрытием
bun run test:coverage
```

## 📚 Документация

### Обновление документации

- Обновляйте README.md для значительных изменений
- Добавляйте JSDoc комментарии к публичным API
- Обновляйте документацию в `docs/`
- Добавляйте примеры использования

### JSDoc комментарии

```typescript
/**
 * Компонент кнопки с различными вариантами
 * 
 * @param props - Свойства компонента
 * @param props.variant - Вариант отображения кнопки
 * @param props.onClick - Обработчик клика
 * @param props.children - Содержимое кнопки
 * 
 * @example
 * ```tsx
 * <Button variant="primary" onClick={() => console.log('clicked')}>
 *   Click me
 * </Button>
 * ```
 */
export function Button({ variant, onClick, children }: ButtonProps) {
  // ...
}
```

## 🎯 Приоритеты

### High Priority

- Исправления критических багов
- Проблемы безопасности
- Проблемы производительности
- Документация

### Medium Priority

- Новые функции
- Улучшения UX
- Рефакторинг кода
- Дополнительные тесты

### Low Priority

- Косметические изменения
- Незначительные оптимизации
- Дополнительные примеры

## 🤔 Вопросы?

Если у вас есть вопросы:

- Откройте [Discussion](https://github.com/yourusername/modern-react-stack/discussions)
- Напишите в [Discord](https://discord.gg/example)
- Отправьте email: support@example.com

## 🙏 Спасибо!

Спасибо за ваш вклад в Modern React Stack! Каждый вклад, большой или маленький, ценится.

---

Happy coding! 🚀
