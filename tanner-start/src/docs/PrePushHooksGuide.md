# Pre-Push Hooks Guide

Руководство по настройке и использованию pre-push хуков для обеспечения качества кода.

## Обзор

Pre-push хуки выполняются автоматически перед отправкой изменений в удаленный репозиторий и включают:

- Полную проверку линтинга и форматирования
- Проверку типов TypeScript
- Запуск всех тестов (unit и integration)
- Проверку покрытия тестами
- Сборку production версии
- Анализ размера bundle
- Проверку безопасности зависимостей
- Проверку дублирования кода

## Конфигурация

### Lefthook конфигурация

```yaml
# lefthook.yml
pre-push:
  parallel: false # Последовательное выполнение
  
  commands:
    lint-full:
      run: npm run lint
      fail_text: "❌ Linting failed"
      
    type-check-full:
      run: npm run type-check
      fail_text: "❌ Type checking failed"
      
    test-unit:
      run: npm run test:unit
      fail_text: "❌ Unit tests failed"
      
    test-integration:
      run: npm run test:integration
      fail_text: "❌ Integration tests failed"
      
    coverage-check:
      run: npm run test:coverage:check
      fail_text: "❌ Coverage below threshold"
      
    build-check:
      run: npm run build
      fail_text: "❌ Production build failed"
```

### Доступные скрипты

```bash
# Полная проверка (как pre-push)
npm run pre-push

# Быстрая проверка (основные проверки)
npm run quick-check

# Отдельные проверки
npm run lint
npm run type-check
npm run test:unit
npm run test:integration
npm run test:coverage:check
npm run build
npm run build:analyze
npm run check:duplicates
```

## Типы проверок

### 1. Линтинг и форматирование

```bash
# Проверка стиля кода
npm run lint

# Проверка форматирования
npm run format:check

# Автоматическое исправление
npm run lint:fix
npm run format
```

**Что проверяется:**
- Стиль кода согласно правилам Biome
- Потенциальные ошибки и проблемы
- Неиспользуемые переменные и импорты
- Соответствие форматированию

### 2. Проверка типов

```bash
npm run type-check
```

**Что проверяется:**
- Корректность TypeScript типов
- Совместимость типов
- Отсутствие ошибок компиляции
- Правильность импортов и экспортов

### 3. Unit тесты

```bash
npm run test:unit
```

**Конфигурация:** `vitest.unit.config.ts`

**Что тестируется:**
- Отдельные функции и компоненты
- Утилиты и хелперы
- Хуки и контексты
- Изолированная логика

**Особенности:**
- Быстрое выполнение (таймаут 5 сек)
- Параллельное выполнение
- Высокие требования к покрытию (80%)

### 4. Integration тесты

```bash
npm run test:integration
```

**Конфигурация:** `vitest.integration.config.ts`

**Что тестируется:**
- Взаимодействие компонентов
- API интеграция
- Полные пользовательские сценарии
- Работа с провайдерами

**Особенности:**
- Более длительное выполнение (таймаут 15 сек)
- Последовательное выполнение
- Реальные сценарии использования

### 5. Проверка покрытия

```bash
npm run test:coverage:check
```

**Пороги покрытия:**
- Branches: 70%
- Functions: 70%
- Lines: 70%
- Statements: 70%

**Исключения:**
- Тестовые файлы
- Конфигурационные файлы
- Типы и интерфейсы
- Моки и фикстуры

### 6. Сборка production

```bash
npm run build
```

**Что проверяется:**
- Успешная сборка для production
- Отсутствие ошибок компиляции
- Правильность импортов
- Оптимизация кода

### 7. Анализ размера bundle

```bash
npm run build:analyze
```

**Пороги размера:**
- Общий размер: < 2MB
- JavaScript: < 1MB
- CSS: < 200KB

**Что анализируется:**
- Размер по типам файлов
- Самые большие файлы
- Процентное соотношение
- Рекомендации по оптимизации

### 8. Проверка безопасности

```bash
npm audit --audit-level=high
```

**Что проверяется:**
- Уязвимости в зависимостях
- Критические и высокие угрозы
- Рекомендации по обновлению
- Статус безопасности пакетов

### 9. Проверка дублирования

```bash
npm run check:duplicates
```

**Конфигурация:** `.jscpd.json`

**Параметры:**
- Минимум 5 строк для дублирования
- Минимум 50 токенов
- Порог дублирования: 3%
- Исключения: тесты, моки, конфиги

## Использование

### Автоматическое выполнение

Pre-push хуки выполняются автоматически при:

```bash
git push
git push origin main
git push --set-upstream origin feature-branch
```

### Ручное выполнение

```bash
# Полная проверка как pre-push
npm run pre-push

# Быстрая проверка
npm run quick-check

# Тестирование pre-push хука
lefthook run pre-push

# Отдельные проверки
npm run lint
npm run test:unit
npm run build
```

### Пропуск хуков

```bash
# Пропустить все хуки
git push --no-verify

# Отключить Lefthook
LEFTHOOK=0 git push

# Пропустить конкретные команды
LEFTHOOK_EXCLUDE=test-unit,test-integration git push
```

## Оптимизация производительности

### Параллельное выполнение

Pre-commit хуки выполняются параллельно для скорости:

```yaml
pre-commit:
  parallel: true  # Быстрые проверки параллельно
```

Pre-push хуки выполняются последовательно для стабильности:

```yaml
pre-push:
  parallel: false  # Полные проверки последовательно
```

### Кеширование

```bash
# Очистка кешей для чистой проверки
npm run cache:clear

# Кеширование в CI
- uses: actions/cache@v3
  with:
    path: ~/.npm
    key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
```

### Условное выполнение

```yaml
# Выполнять только при изменении определенных файлов
test-unit:
  files: git diff --name-only HEAD~1 HEAD
  glob: "src/**/*.{ts,tsx}"
  run: npm run test:unit
```

## Отладка и устранение проблем

### Просмотр логов

```bash
# Подробный вывод
lefthook run pre-push --verbose

# Логи последнего выполнения
cat .lefthook/logs/pre-push.log
```

### Частые проблемы

1. **Тесты падают локально**
   ```bash
   # Очистить кеши
   npm run cache:clear
   
   # Переустановить зависимости
   rm -rf node_modules package-lock.json
   npm install
   ```

2. **Проблемы с типами**
   ```bash
   # Перегенерировать типы
   npm run type-check
   
   # Проверить конфигурацию TypeScript
   npx tsc --showConfig
   ```

3. **Ошибки сборки**
   ```bash
   # Очистить dist
   rm -rf dist
   npm run build
   
   # Проверить переменные окружения
   cat .env.example
   ```

### Диагностика

```bash
# Проверка состояния хуков
npm run hooks:check

# Тестирование конфигурации
lefthook dump

# Проверка отдельных команд
npm run lint
npm run type-check
npm run test:unit
```

## Настройка для команды

### Локальные переопределения

Создайте `lefthook-local.yml`:

```yaml
pre-push:
  commands:
    # Отключить медленные проверки для разработки
    test-integration:
      skip: true
    
    build-check:
      skip: true
    
    # Использовать быстрые альтернативы
    test-unit:
      run: npm run test:unit --run --reporter=basic
```

### Переменные окружения

```bash
# Отключить определенные проверки
export LEFTHOOK_EXCLUDE=test-integration,build-check

# Ускорить тесты
export NODE_OPTIONS="--max-old-space-size=4096"

# Отключить покрытие для скорости
export SKIP_COVERAGE=true
```

### CI/CD интеграция

```yaml
# .github/workflows/ci.yml
- name: Run pre-push checks
  run: |
    npm run hooks:install
    npm run pre-push
  env:
    CI: true
    NODE_ENV: test
```

## Мониторинг и метрики

### Время выполнения

```bash
# Измерение времени каждой проверки
time npm run lint
time npm run test:unit
time npm run build
```

### Статистика покрытия

```bash
# Генерация отчета покрытия
npm run test:coverage

# Просмотр HTML отчета
open coverage/index.html
```

### Размер bundle

```bash
# Анализ размера
npm run build:analyze

# Сравнение с предыдущими версиями
npm run build:analyze > bundle-report.txt
```

## Лучшие практики

### 1. Быстрые pre-commit, полные pre-push

```yaml
pre-commit:
  # Только быстрые проверки
  commands:
    lint-staged: # Только измененные файлы
    type-check: # Быстрая проверка
    test-related: # Только связанные тесты

pre-push:
  # Полные проверки
  commands:
    lint-full: # Весь проект
    test-full: # Все тесты
    build-check: # Полная сборка
```

### 2. Прогрессивные проверки

```yaml
pre-push:
  commands:
    # Сначала быстрые проверки
    lint-full: # ~10 сек
    type-check: # ~15 сек
    
    # Затем тесты
    test-unit: # ~30 сек
    test-integration: # ~60 сек
    
    # В конце тяжелые операции
    build-check: # ~45 сек
    coverage-check: # ~20 сек
```

### 3. Информативные сообщения

```yaml
commands:
  test-unit:
    run: npm run test:unit
    fail_text: |
      ❌ Unit tests failed!
      
      💡 Tips:
      • Run 'npm run test:unit' to see detailed output
      • Run 'npm run test:watch' for interactive testing
      • Check test files for syntax errors
```

### 4. Условные проверки

```yaml
# Проверять покрытие только для основных веток
coverage-check:
  run: npm run test:coverage:check
  skip: |
    [[ "$LEFTHOOK_BRANCH" != "main" && "$LEFTHOOK_BRANCH" != "develop" ]]
```

## Заключение

Pre-push хуки обеспечивают:

- ✅ Высокое качество кода в репозитории
- ✅ Предотвращение попадания багов в main ветку
- ✅ Автоматическую проверку всех аспектов проекта
- ✅ Единообразие стандартов в команде
- ✅ Раннее обнаружение проблем

Следуйте принципам:
- Делайте pre-commit быстрыми (< 30 сек)
- Делайте pre-push полными (< 5 мин)
- Предоставляйте четкие сообщения об ошибках
- Позволяйте локальные переопределения
- Мониторьте производительность хуков