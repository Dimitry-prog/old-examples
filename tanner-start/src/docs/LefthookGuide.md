# Lefthook Git Hooks Guide

Руководство по использованию Lefthook для автоматизации проверок качества кода.

## Обзор

Lefthook - современный менеджер Git hooks, который обеспечивает:
- Автоматические проверки перед коммитом и пушем
- Параллельное выполнение команд
- Гибкую конфигурацию
- Кроссплатформенность
- Высокую производительность

## Установка и настройка

### Автоматическая настройка

```bash
npm run hooks:setup
```

Этот скрипт автоматически:
- Проверит наличие Git репозитория
- Установит Lefthook hooks
- Проверит конфигурацию
- Обновит .gitignore

### Ручная настройка

```bash
# Установка hooks
npm run hooks:install

# Удаление hooks
npm run hooks:uninstall

# Проверка конфигурации
lefthook dump
```

## Конфигурация

### Основной файл: lefthook.yml

```yaml
pre-commit:
  parallel: true
  commands:
    lint-staged:
      glob: "*.{js,jsx,ts,tsx}"
      run: npm run lint:staged
      stage_fixed: true
    
    type-check:
      glob: "*.{ts,tsx}"
      run: npm run type-check

pre-push:
  commands:
    test-full:
      run: npm run test:ci
    
    build-check:
      run: npm run build
```

### Локальная конфигурация: lefthook-local.yml

```yaml
# Отключить определенные хуки для локальной разработки
pre-commit:
  commands:
    type-check:
      skip: true
```

## Доступные хуки

### Pre-commit (перед коммитом)

Выполняется автоматически при `git commit`:

- **lint-staged** - Линтинг только измененных файлов
- **format-check** - Проверка форматирования
- **type-check** - Проверка типов TypeScript
- **test-related** - Тесты для связанных файлов
- **check-commit-size** - Проверка размера коммита
- **check-merge-conflicts** - Проверка конфликтов слияния

### Pre-push (перед отправкой)

Выполняется автоматически при `git push`:

- **lint-full** - Полная проверка линтинга
- **type-check-full** - Полная проверка типов
- **test-full** - Запуск всех тестов
- **build-check** - Проверка сборки
- **coverage-check** - Проверка покрытия тестов

### Commit-msg (проверка сообщения)

Выполняется при создании коммита:

- **commitlint** - Проверка формата сообщения коммита

### Post-commit (после коммита)

- **notify** - Уведомление об успешном коммите

### Post-checkout (после переключения веток)

- **install-deps** - Установка зависимостей при изменении package.json
- **clear-cache** - Очистка кеша

### Post-merge (после слияния)

- **install-deps** - Установка зависимостей
- **migrate** - Запуск миграций (если есть)

## Команды

### Основные команды

```bash
# Установка hooks
lefthook install

# Удаление hooks
lefthook uninstall

# Просмотр конфигурации
lefthook dump

# Запуск конкретного хука
lefthook run pre-commit
lefthook run pre-push

# Запуск с дополнительными параметрами
lefthook run pre-commit --no-tty
```

### NPM скрипты

```bash
# Настройка hooks
npm run hooks:setup

# Установка/удаление
npm run hooks:install
npm run hooks:uninstall

# Запуск hooks
npm run hooks:run pre-commit
npm run hooks:run pre-push
```

## Пропуск хуков

### Временное отключение

```bash
# Пропустить все хуки для одного коммита
git commit --no-verify

# Пропустить все хуки для одного пуша
git push --no-verify

# Отключить Lefthook полностью
LEFTHOOK=0 git commit
```

### Отключение конкретных команд

```bash
# Пропустить конкретную команду
LEFTHOOK_EXCLUDE=lint-staged git commit

# Пропустить несколько команд
LEFTHOOK_EXCLUDE=lint-staged,type-check git commit
```

### Локальное отключение

Создайте файл `lefthook-local.yml`:

```yaml
pre-commit:
  commands:
    type-check:
      skip: true
    test-related:
      skip: true
```

## Отладка

### Просмотр выполняемых команд

```bash
# Подробный вывод
lefthook run pre-commit --verbose

# Только команды без выполнения
lefthook run pre-commit --no-tty
```

### Проверка конфигурации

```bash
# Валидация конфигурации
lefthook dump

# Список доступных хуков
lefthook list
```

### Логи

```bash
# Просмотр логов последнего выполнения
cat .lefthook/logs/pre-commit.log
```

## Настройка для команды

### Глобальные настройки

```yaml
# lefthook.yml
settings:
  skip_on_branches:
    - main
    - master
  skip_on_users:
    - ci
    - github-actions
```

### Переменные окружения

```yaml
env:
  NODE_ENV: development
  CI: true
  NODE_OPTIONS: "--max-old-space-size=4096"
```

## Интеграция с CI/CD

### GitHub Actions

```yaml
# .github/workflows/ci.yml
- name: Install dependencies
  run: npm ci

- name: Install Lefthook
  run: npm run hooks:install

- name: Run pre-push checks
  run: lefthook run pre-push
```

### Отключение в CI

```yaml
# lefthook.yml
settings:
  skip_on_ci: true
```

Или через переменную окружения:

```bash
export LEFTHOOK=0
```

## Кастомные скрипты

### Встроенные скрипты

```yaml
scripts:
  check-branch.sh: |
    #!/bin/bash
    branch=$(git branch --show-current)
    if [[ $branch == "main" ]]; then
      echo "Direct commits to main are not allowed"
      exit 1
    fi
```

### Внешние скрипты

```yaml
pre-commit:
  scripts:
    "scripts/custom-check.sh":
      runner: bash
```

## Производительность

### Параллельное выполнение

```yaml
pre-commit:
  parallel: true  # Команды выполняются параллельно
  
pre-push:
  parallel: false # Команды выполняются последовательно
```

### Оптимизация для больших проектов

```yaml
pre-commit:
  commands:
    lint-staged:
      # Только для staged файлов
      files: git diff --cached --name-only --diff-filter=ACMR
      
    test-related:
      # Только связанные тесты
      run: npm run test:related
```

## Устранение проблем

### Частые проблемы

1. **Hooks не выполняются**
   ```bash
   # Переустановите hooks
   lefthook uninstall
   lefthook install
   ```

2. **Ошибки прав доступа**
   ```bash
   # Сделайте скрипты исполняемыми
   chmod +x .git/hooks/*
   ```

3. **Конфликты с другими hook менеджерами**
   ```bash
   # Удалите старые hooks
   rm -rf .git/hooks/*
   lefthook install
   ```

### Диагностика

```bash
# Проверка статуса
lefthook version
lefthook dump

# Тестирование hooks
lefthook run pre-commit --no-tty
```

## Лучшие практики

### 1. Быстрые pre-commit хуки

```yaml
pre-commit:
  commands:
    # Только для staged файлов
    lint-staged:
      glob: "*.{js,jsx,ts,tsx}"
      run: npm run lint:staged
      stage_fixed: true
    
    # Быстрая проверка типов
    type-check:
      run: npm run type-check:fast
```

### 2. Полные pre-push хуки

```yaml
pre-push:
  commands:
    # Полная проверка перед отправкой
    full-check:
      run: npm run ci:check
```

### 3. Условное выполнение

```yaml
pre-commit:
  commands:
    migrate:
      # Только если изменились миграции
      files: git diff --cached --name-only
      glob: "migrations/**/*"
      run: npm run db:migrate
```

### 4. Уведомления

```yaml
post-commit:
  commands:
    notify:
      run: |
        echo "✅ Commit successful!"
        echo "📝 Files changed: $(git diff --name-only HEAD~1 HEAD | wc -l)"
```

## Заключение

Lefthook обеспечивает:

- ✅ Автоматические проверки качества кода
- ✅ Предотвращение попадания багов в репозиторий
- ✅ Единообразие в команде
- ✅ Высокую производительность
- ✅ Гибкую настройку

Следуйте принципам:
- Делайте pre-commit хуки быстрыми
- Используйте pre-push для полных проверок
- Настраивайте локальные конфигурации
- Документируйте кастомные хуки
- Тестируйте конфигурацию регулярно