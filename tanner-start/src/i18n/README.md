# Lingui i18n Integration

Руководство по использованию интернационализации (i18n) в проекте с помощью Lingui.

## Содержание

- [Обзор](#обзор)
- [Быстрый старт](#быстрый-старт)
- [Использование](#использование)
- [Команды CLI](#команды-cli)
- [Миграция компонентов](#миграция-компонентов)
- [Лучшие практики](#лучшие-практики)

## Обзор

Проект использует [Lingui](https://lingui.dev/) для интернационализации:

- **Легковесность**: ~3 KB в production
- **TypeScript**: Полная типобезопасность
- **ICU MessageFormat**: Плюрализация и интерполяция
- **Автоэкстракция**: Автоматическое извлечение сообщений из кода
- **Роутинг**: Локаль управляется через URL (`/ru/dashboard`, `/en/dashboard`)

### Поддерживаемые языки

- 🇷🇺 Русский (ru) - исходный язык
- 🇬🇧 Английский (en)

## Быстрый старт

### 1. Использование в компонентах

```tsx
import { Trans } from '@lingui/react/macro';

function MyComponent() {
  return (
    <div>
      <h1><Trans>Привет, мир!</Trans></h1>
    </div>
  );
}
```

### 2. Извлечение переводов

```bash
npm run i18n:extract
```

### 3. Добавление переводов

Откройте `src/locales/en/messages.po` и добавьте переводы:

```po
msgid "Привет, мир!"
msgstr "Hello, world!"
```

### 4. Компиляция

```bash
npm run i18n:compile
```

## Использование

### 1. Переводы в JSX - `<Trans>`

Используйте для текстового контента в JSX:

```tsx
import { Trans } from '@lingui/react/macro';

// ✅ Простой текст
<h1><Trans>Добро пожаловать</Trans></h1>

// ✅ С переменными
<p><Trans>Привет, {name}!</Trans></p>

// ✅ С вложенными элементами
<p>
  <Trans>
    У вас <strong>{count}</strong> непрочитанных сообщений
  </Trans>
</p>

// ✅ С JSX элементами
<p>
  <Trans>
    Прочитайте <a href="/docs">документацию</a> для деталей
  </Trans>
</p>
```

**Когда использовать:**
- Текстовый контент в JSX
- Сообщения с вложенными React компонентами
- Основной способ перевода в компонентах

### 2. Плюрализация - `<Plural>`

Используйте для правильных форм слов в зависимости от числа:

```tsx
import { Plural } from '@lingui/react/macro';

<Plural
  value={count}
  one="# день"
  few="# дня"
  many="# дней"
  other="# дней"
/>
```

**Формы для русского языка:**
- `one`: 1, 21, 31, ... (# день)
- `few`: 2-4, 22-24, ... (# дня)
- `many`: 5-20, 25-30, ... (# дней)
- `other`: fallback

**Формы для английского:**
- `one`: 1 (# day)
- `other`: все остальные (# days)

**Когда использовать:**
- Счетчики с единицами измерения
- Сообщения с количеством элементов

### 3. Переводы в атрибутах - `t` макрос

Используйте для атрибутов HTML (placeholder, title, aria-label):

```tsx
import { t } from '@lingui/core/macro';
import { useLingui } from '@lingui/react';

function MyComponent() {
  const { i18n } = useLingui();
  
  return (
    <input
      placeholder={t(i18n)`Введите имя`}
      title={t(i18n)`Это поле обязательно`}
      aria-label={t(i18n)`Имя пользователя`}
    />
  );
}
```

**Когда использовать:**
- Атрибуты HTML элементов
- Строковые значения в props
- Любые места, где нужна строка, а не JSX

### 4. Переводы вне React - хелпер `l()`

Используйте для переводов вне компонентов или с ручными ключами:

```tsx
import { l } from '@/i18n/helpers';

// В утилитах, константах, конфигах
const errorMessage = l('errors.network');

// С параметрами
const greeting = l('user.greeting', { name: 'John' });

// В обработчиках событий
const handleClick = () => {
  alert(l('messages.success'));
};
```

**⚠️ Важно:** Для `l()` нужно вручную добавлять ключи в `messages.po`:

```po
msgid "errors.network"
msgstr "Ошибка сети"

msgid "user.greeting"
msgstr "Привет, {name}!"
```

**Когда использовать:**
- Переводы вне React компонентов
- Утилиты, константы, конфигурационные файлы
- Когда нужен простой синтаксис с ключами

### Сравнение подходов

| Подход | Где использовать | Автоэкстракция | Пример |
|--------|------------------|----------------|--------|
| `<Trans>` | JSX контент | ✅ Да | `<Trans>Текст</Trans>` |
| `<Plural>` | Плюрализация в JSX | ✅ Да | `<Plural value={n} one="# день" />` |
| `t(i18n)` | Атрибуты, строки в React | ✅ Да | `t(i18n)\`Текст\`` |
| `l()` | Вне React, ручные ключи | ❌ Нет | `l('key', { param })` |

## Команды CLI

### Извлечение сообщений

Сканирует код и извлекает все сообщения в `.po` файлы:

```bash
npm run i18n:extract
```

Автоматически находит:
- `<Trans>` компоненты
- `<Plural>` компоненты  
- `t` макросы
- Обновляет `src/locales/{locale}/messages.po`

### Компиляция каталогов

Компилирует `.po` файлы в оптимизированный JS формат:

```bash
npm run i18n:compile
```

Создает `src/locales/{locale}/messages.js` для production.

### Watch режим

Автоматически извлекает сообщения при изменении файлов:

```bash
npm run i18n:extract:watch
```

### Перед сборкой

Автоматически компилирует переводы перед production сборкой:

```bash
npm run build  # автоматически запускает i18n:compile
```

## Миграция компонентов

### Шаг 1: Добавить импорты

```tsx
import { Trans } from '@lingui/react/macro';
import { t } from '@lingui/core/macro';
import { useLingui } from '@lingui/react';
```

### Шаг 2: Обернуть тексты

**До:**
```tsx
<h1>Добро пожаловать</h1>
<p>Привет, {name}!</p>
<input placeholder="Введите имя" />
```

**После:**
```tsx
const { i18n } = useLingui();

<h1><Trans>Добро пожаловать</Trans></h1>
<p><Trans>Привет, {name}!</Trans></p>
<input placeholder={t(i18n)`Введите имя`} />
```

### Шаг 3: Извлечь и перевести

```bash
npm run i18n:extract
# Добавить переводы в src/locales/en/messages.po
npm run i18n:compile
```

## Лучшие практики

### ✅ DO

```tsx
// Используйте Trans для JSX
<h1><Trans>Заголовок</Trans></h1>

// Используйте t для атрибутов
<input placeholder={t(i18n)`Текст`} />

// Используйте Plural для чисел
<Plural value={count} one="# день" other="# дней" />

// Переменные в Trans
<Trans>Привет, {name}!</Trans>

// JSX элементы в Trans
<Trans>Читайте <a href="/docs">документацию</a></Trans>
```

### ❌ DON'T

```tsx
// ❌ Не конкатенируйте строки
<Trans>Привет, </Trans>{name}

// ❌ Не используйте шаблонные строки в Trans
<Trans>{`Привет, ${name}`}</Trans>

// ❌ Не забывайте i18n в t макросе
<input placeholder={t`Текст`} />  // Неправильно!

// ❌ Не используйте Trans для атрибутов
<input placeholder={<Trans>Текст</Trans>} />  // Ошибка!
```

### Советы

1. **Контекст важен**: Добавляйте комментарии для переводчиков:
   ```tsx
   {/* i18n: Кнопка для отправки формы */}
   <button><Trans>Отправить</Trans></button>
   ```

2. **Избегайте разбиения**: Держите предложения целыми:
   ```tsx
   // ✅ Хорошо
   <Trans>Вы уверены, что хотите удалить {count} элементов?</Trans>
   
   // ❌ Плохо
   <Trans>Вы уверены, что хотите удалить</Trans> {count} <Trans>элементов?</Trans>
   ```

3. **Используйте ID для сложных случаев**:
   ```tsx
   <Trans id="dashboard.welcome.title">Добро пожаловать</Trans>
   ```

4. **Тестируйте оба языка**: Проверяйте UI на обоих языках - длина текста может отличаться.

## Структура файлов

```
src/
├── i18n/
│   ├── index.ts          # Инициализация i18n
│   ├── config.ts         # Конфигурация локалей
│   ├── types.ts          # TypeScript типы
│   ├── utils.ts          # Утилиты (detectLocale, saveLocale)
│   ├── helpers.ts        # Хелпер l()
│   └── README.md         # Эта документация
├── locales/
│   ├── ru/
│   │   ├── messages.po   # Русские переводы (исходник)
│   │   └── messages.js   # Скомпилированные (генерируется)
│   └── en/
│       ├── messages.po   # Английские переводы
│       └── messages.js   # Скомпилированные (генерируется)
└── routes/
    └── $locale/          # Роуты с локалью в URL
        └── _layout.tsx   # Layout с I18nProvider
```

## Переключение языка

Язык переключается через URL и компонент `LanguageSwitcher`:

```tsx
import { LanguageSwitcher } from '@/components/common/LanguageSwitcher';

<LanguageSwitcher />
```

- Локаль сохраняется в `localStorage`
- При первом визите определяется из браузера
- URL: `/ru/dashboard` → `/en/dashboard`

## Примеры

Полные примеры использования смотрите в:
- `src/components/examples/I18nExample.tsx` - все варианты использования
- Роут: `/examples/i18n` (требуется авторизация)

## Дополнительные ресурсы

- [Lingui Documentation](https://lingui.dev/)
- [ICU MessageFormat](https://unicode-org.github.io/icu/userguide/format_parse/messages/)
- [React Intl vs Lingui](https://lingui.dev/misc/react-intl)

## Troubleshooting

### Переводы не применяются

1. Проверьте, что запустили `npm run i18n:compile`
2. Убедитесь, что файлы `.js` созданы в `src/locales/{locale}/`
3. Перезапустите dev сервер

### Ошибка "i18n._ is not a function"

Убедитесь, что компонент обернут в `<I18nProvider>` (уже настроено в `$locale/_layout.tsx`).

### Макросы не работают

Проверьте, что в `vite.config.ts` подключен `@lingui/vite-plugin`.

### Новые сообщения не извлекаются

1. Проверьте, что файл не исключен в `lingui.config.ts`
2. Используйте правильные макросы (`Trans`, `t`, `Plural`)
3. Запустите `npm run i18n:extract` заново
