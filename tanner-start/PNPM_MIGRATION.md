# Миграция на pnpm

Этот проект теперь использует только **pnpm** как пакетный менеджер для обеспечения консистентности и производительности.

## 🚀 Быстрый старт

```bash
# Установить pnpm глобально
npm install -g pnpm

# Установить зависимости
pnpm install

# Запустить проект
pnpm run dev
```

## ⚠️ Важные изменения

### Заблокированные менеджеры
- ❌ `npm install` - заблокирован
- ❌ `yarn install` - заблокирован  
- ❌ `bun install` - заблокирован
- ✅ `pnpm install` - единственный разрешенный

### Автоматическая очистка
При попытке использования других менеджеров:
- Автоматически удаляются `package-lock.json`, `yarn.lock`, `bun.lockb`
- Очищается `node_modules` если найдены lock файлы других менеджеров
- Показывается понятное сообщение об ошибке

## 📋 Обновленные команды

Все команды теперь используют `pnpm run` вместо `bun run`:

```bash
# Разработка
pnpm run dev              # Запуск dev сервера
pnpm run build            # Сборка для продакшена
pnpm run preview          # Предпросмотр продакшен сборки

# Тестирование
pnpm run test             # Запуск тестов
pnpm run test:ui          # UI для тестов
pnpm run test:coverage    # Покрытие тестов

# Качество кода
pnpm run lint             # Проверка кода
pnpm run lint:fix         # Исправление ошибок
pnpm run format           # Форматирование кода
```

## 🔧 Настройки pnpm

Проект включает оптимизированную конфигурацию в `.npmrc`:
- Автоматическая установка peer dependencies
- Изолированный node-linker для безопасности
- Настройки кэша для производительности

## 🐳 Docker

Dockerfile обновлен для использования pnpm:

```dockerfile
FROM node:18-alpine as builder

# Установить pnpm
RUN npm install -g pnpm

WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile

COPY . .
RUN pnpm run build
```

## 🤔 Почему pnpm?

- **Производительность**: Быстрее установка и меньше места на диске
- **Безопасность**: Строгая изоляция зависимостей
- **Консистентность**: Один менеджер для всей команды
- **Монорепозитории**: Готовность к масштабированию

## 🆘 Помощь

Если у вас проблемы с установкой pnpm:

```bash
# Через npm
npm install -g pnpm

# Через Homebrew (macOS)
brew install pnpm

# Через Chocolatey (Windows)
choco install pnpm

# Через Scoop (Windows)
scoop install pnpm
```

Подробнее: https://pnpm.io/installation
